# Copy this file to a new file named  .env  and fill in a real key ONLY if you
# switch to the paid OpenRouter fallback. The free local Ollama version needs
# no key at all, so you can ignore this unless you go paid.
OPENROUTER_API_KEY=your_key_here
